PlayGuideWindow("This screen shows details on your income tax policy, including the effects it has on other items. Depending on your priorities, you may want to increase or reduce the tax. You can adjust it by using the slider, and can see the difference your changes will make. Note that some policies have an 'implementation time' which means it takes a while for your changes to a slider to actually take effect, and also some of the effects themselves have 'inertia' so it may be a while until you see the full results.",20,200,TRUE);CloseVoterDetails();SetPlayGuideFlag(HIDESITUATIONICONS,FALSE);SetPlayGuideFlag(HIDEVOTERICONS,FALSE);SetPlayGuideFlag(HIDESIMICONS,FALSE);ClearPermittedIcons();ClearAllowedButtons();