SetPlayGuideFlag(HIDEPOLICYICONS,FALSE);SetPlayGuideFlag(HIDESITUATIONICONS,FALSE);SetPlayGuideFlag(HIDEVOTERICONS,FALSE);SetPlayGuideFlag(HIDESIMICONS,FALSE);SetPlayGuideFlag(HIDEPOPBAR,FALSE);SetPlayGuideFlag(DISABLEICONCONNECTORS,FALSE);PlayGuideWindow("Hovering your mouse over any of the icons we have seen so far will highlight that object and draw links to those others that affect it, and which it in turn affects. You can try this now. The colors of the lines denote positive effects (green) and negative (red). Be aware that sometimes a negative effect is desirable (such as reducing crime). The speed of the line shows the intensity of the effect, but we will look at more accurate information on this later.",20,50,TRUE);